# Smart Campus — Lost & Found System

Minimal repo scaffolding for a Next.js 14 + TypeScript + Tailwind CSS application.

Quick start

1. Install dependencies:

```bash
npm install
```

2. Run dev server:

```bash
npm run dev
```

This repository contains a scalable `src/` layout using the Next.js App Router.

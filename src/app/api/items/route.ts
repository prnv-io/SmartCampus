// API route placeholder: /api/items
// Implementation will handle item CRUD and listing

import { NextResponse } from 'next/server'

export async function GET() {
  return NextResponse.json({ message: 'items route placeholder' })
}
